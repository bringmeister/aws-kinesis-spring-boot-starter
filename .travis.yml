language: java
jdk:
  - oraclejdk8

sudo: required
before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar

script:
  - ./gradlew check

after_success:
  - ./gradlew jacocoTestReport coveralls